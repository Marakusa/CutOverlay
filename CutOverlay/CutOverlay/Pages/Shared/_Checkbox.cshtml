@using InputModel = CutOverlay.Models.InputModel
@model InputModel

<div class="settingsInput checkboxInput">
    <input type="checkbox" id="@Model.Id" checked="@Model.Value">
    <span class="label">@Model.Label</span>
</div>